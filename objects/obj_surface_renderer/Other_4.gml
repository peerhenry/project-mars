/// @description Insert description here

// This is needed for when the player comes back from mission control
if(use_shader)
{
	application_surface_draw_enable(false);
}
else
{
	application_surface_draw_enable(true);
}