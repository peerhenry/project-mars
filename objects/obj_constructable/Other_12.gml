/// @description Finalize construction

under_construction = false;
scr_grid_logic(id);

// show_debug_message("Constructable is finalized for " + scr_object_index_string(id) + " and has following grid props: "); // DEBUG
// scr_debug_show_grid_props(id);